{
  "name": "vending-voice-algorithm-local-test-bundle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "uv run --directory backend --extra test pytest && npm --prefix frontend test",
    "build": "npm --prefix frontend run build"
  }
}
